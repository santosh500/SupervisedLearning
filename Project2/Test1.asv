vertArray2 = [];
compare1 = [];
compare2 = [];
for i = 26:40
    g1 = [];
    for j = 1:5
        %y = dir(strcat('att_faces\s',i,'.pgm'));
        r = strcat('att_faces\s',int2str(i));
        y = strcat(r,'\',int2str(j),'.pgm');
        v = imread(y);
        r = reshape(v', [], 1);
        g1 = horzcat(g,r);
    end
    vertArray2 = horzcat(vertArray2,g);
end